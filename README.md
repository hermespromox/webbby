# Webbby

Webbby analyses any website against a custom key/value criteria list and returns strict JSON through OpenRouter structured outputs.

## Local dev

```bash
npm install
npm run dev
```

## Environment

Set `OPENROUTER_API_KEY` on Vercel. The key is only used server-side by `api/analyze.js`.
